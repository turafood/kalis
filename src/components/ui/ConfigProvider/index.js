import ConfigProvider, {
    ConfigContext,
    ConfigConsumer,
    useConfig,
    defaultConfig,
} from './ConfigProvider'

export {
    ConfigProvider,
    ConfigContext,
    ConfigConsumer,
    useConfig,
    defaultConfig,
}

export default ConfigProvider
